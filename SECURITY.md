# Security Policy

## 报告漏洞

公开仓库建立后，请优先使用 GitHub 的 Private vulnerability reporting 或
Security Advisory 私下报告漏洞。不要在公开 Issue 中发布可用的 API Key、
用户文件、数据库、日志或可直接利用的细节。

## 凭证与本地数据

ArHub 的模型凭证保存在本机用户设置数据库中。该数据库不属于源码，也不应
进入备份仓库。若凭证曾出现在聊天、日志、截图或 Git 历史中，应立即在服务商
控制台吊销并重新生成，仅从当前文件中删除并不能使旧凭证失效。

## Agent 风险边界

Agent 工作流能够读写工作区文件并启动本地工具。这是核心能力，也是高权限
行为。只应打开可信工作区、使用可信 Skill 和扩展，并在执行前审查模型建议的
命令。模型服务商可能接收到提示词和任务所需内容，请按数据敏感级别选择服务。

声明式扩展注册表当前只读取 JSON manifest，不导入或执行第三方扩展代码。
若未来增加可执行扩展，必须先设计权限声明、隔离、签名验证和撤销机制。

## 支持范围

当前恢复分支属于预发布软件。安全修复以最新 main 为准；尚未提供长期维护
版本或公开安装包更新通道。
